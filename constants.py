
def _get_digits() -> int:
    """
    Get the number of decimal digits for rounding.

    Returns:
        int: The number of decimal digits (default is 2).
    """
    digits: int = 2
    return digits


def _get_max_tries() -> int:
    """
    Get the number of maximal tries for inputting a run.

    Returns:
        int: The number of maximal tires (default is 100).
    """
    max_tries: int = 100
    return max_tries
